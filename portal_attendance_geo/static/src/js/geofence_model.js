odoo.define('portal_attendance_geo.GeofenceModel', function (require) {
    'use strict';

    var BasicModel = require('web.BasicModel');

    var GeofenceModel = BasicModel.extend({});

    return GeofenceModel;

});
